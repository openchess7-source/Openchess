# Point this at your real Openchess backend.
VITE_API_URL=http://localhost:4000

# Demo mode is OFF by default (production-safe). Set to "true" only to
# preview the app locally without a backend attached — see README.md.
# VITE_DEMO_MODE=true
